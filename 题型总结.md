# 题型总结

> 本仓库为竞赛刷题（Competitive Programming）代码与题解库，题解通过脚本同步到博客 / Notion。
> 解法以 `.cpp`（代码）+ `.md`（题意 / 题解 / 代码）成对存在。
>
> 规模概览：`.cpp` ≈ 1341、`.md` ≈ 834、辅助脚本 ≈ 11。

---

## 一、按来源（顶层目录）划分

| 目录 | 题目来源 | 组织方式 | 题解数(.md) |
|------|----------|----------|------------|
| `leetcode/` | 力扣 | **按算法题型分类**(28 个分类目录) | ~346 |
| `cf/` | Codeforces | 按比赛 / 每日一题 / 专项练习 | ~440 |
| `atcode/` | AtCoder | 按比赛(abcXXX) | ~22 |
| `0x3f/` | 灵神(0x3f)题单 | 按题号(平铺) | ~25 |
| `template/` | 算法模板库 | 按数据结构 / 算法分类 | — |
| `script/` | 数据与工具 | 抓题、生成 md、同步博客 | — |

---

## 二、LeetCode —— 按算法题型分类(核心分类体系)

`leetcode/` 下共 **28 个题型目录**,是本仓库分类最细的部分。按算法思想归类如下:

### 1. 动态规划(`dp/`,含 10 个子类)
最系统的一类,单独建子目录:

| 子目录 | 题型 |
|--------|------|
| `线性dp` | 线性 DP |
| `区间dp` | 区间 DP |
| `背包dp` | 背包问题 |
| `状压dp` | 状态压缩 DP |
| `数位dp` | 数位 DP |
| `树型dp` | 树形 DP |
| `概率期望dp` | 概率 / 期望 DP |
| `博弈dp` | 博弈 DP |
| `LCS` | 最长公共子序列 |
| (根目录散题) | 字符串转换、矩阵中严格递增、划分数组等 |

### 2. 搜索类
- `BFS/`(17) —— 广度优先搜索
- `DFS/`(12) —— 深度优先搜索
- `记忆化搜索/`(3)
- `折半搜索/`(5) —— 双向 / Meet in the middle

### 3. 图论 / 树
- `最短路径/`(10) —— Dijkstra / Floyd / Bellman-Ford
- `拓扑排序/`(1)
- `欧拉路径/`(2)
- `并查集/`(11)
- `倍增法/`(4) —— LCA / 倍增

### 4. 数据结构
- `栈队列堆/`(25) —— 单调栈、优先队列、堆
- `树状数组线段树/`(10)
- `平衡树/`(5)
- `字典树/`(2) —— Trie
- `链表/`(3)

### 5. 字符串
- `字符串/`(1)
- `字符串哈希/`(3)
- `哈希/`(5)

### 6. 区间 / 前缀 / 双指针技巧
- `前缀和/`(6)
- `差分/`(2)
- `区间计数/`(2)
- `滑动窗口双指针/`(12)
- `子序列、子串、数对/`(10)

### 7. 数学 / 思维 / 其他
- `数学/`(0,占位)
- `位运算/`(3)
- `二分法/`(24) —— 二分答案 / 二分查找(题量较大)
- `思维题/`(36) —— **题量最大**,构造、贪心、脑筋急转弯类
- `暴力模拟/`(13)
- `有限状态机/`(11)

> **题量 TOP 分类**:思维题(36) > 栈队列堆(25) > 二分法(24) > BFS(17)。
> 反映该刷题者侧重思维 / 二分 / 数据结构。

---

## 三、Codeforces (`cf/`)

按"练习场景"而非纯算法分类:

| 子目录 | 内容 |
|--------|------|
| `contest/` | 按比赛号归档(136 个比赛目录,如 1440/1453/...),每场内有 `A.cpp`、`B.cpp`、`比赛号.md` |
| `a-problem-everyday/` | **每日一题**,按年/月分目录(2023、2024),抓题脚本在 `script/ape/gen_ape.py` |
| `practice/constructive/` | **构造题专项**,按 rating(1000~1900)分难度档,`classify.py` 调用 CF API 按难度自动归档 |
| `practice/review/` | 复习题 |
| `unsolve/` | 未解决题清单(`unsolve.md`) |

---

## 四、AtCoder (`atcode/`)

- `contest/` —— 按 ABC 比赛号归档(abc275 ~ abc303 等 28 场)
- 每场内含 `A.cpp` ~ `F.cpp` + `abcXXX.md`

---

## 五、0x3f 题单 (`0x3f/`)

- 灵茶山艾府(灵神)题单,**按题号平铺**(如 `1054D.cpp/.md`)
- 同时含 Codeforces 题号(1054D、1095E …)与 AtCoder 题号(arc111_b、arc119_c …)
- 偏专题训练(难度较高)

---

## 六、算法模板库 (`template/`)

按数据结构 / 算法分类的可复用模板:

| 分类 | 模板 |
|------|------|
| **树结构** | `BST`(平衡树)、`LCA`(最近公共祖先)、`treeintree`(树套树) |
| **字符串** | `ac自动机`、`sa`(后缀数组) |
| **图论** | `astar`(A* 搜索)、`割点与桥`、`hpd`(树链剖分) |
| **数据结构** | `线段树与树状数组`(13)、`分块`(数列分块入门 1-8,13)、`可持久化`(主席树等) |
| **数学** | `sieve`(筛法)、`modint`(取模运算)、`modui`(莫队)、`rsa`、`期望`、`slope`(斜率优化) |
| **工具** | `vscode`(配置) |

---

## 七、配套工具脚本

| 脚本 | 作用 |
|------|------|
| `leetcode/lcmd.py` | 通过 LeetCode GraphQL 抓取题目 markdown |
| `script/ape/gen_ape.py` | 每日一题抓取 |
| `cf/practice/constructive/classify.py` | 调用 CF API 按 rating 自动分档归类 |
| `script/hexo/hexo_deployer.py` | 同步题解到 Hexo 博客 |
| `script/ape/update_data.py` | 更新 CF 题库数据(`cf-all.json`) |

`script/` 下各脚本 `--help` 可见完整用法。

---

## 八、题解文件标准格式

每道题的 `.md` 通常包含:

```
# 题目标题 / 链接
rating: 分数      (CF/AtCoder)
tag: 标签

## 题意
## 题解
## 代码
``` cpp
... 解题代码 ...
```
```

---

## 总结:题型分类两个维度

```
维度一(按来源):  LeetCode  |  Codeforces  |  AtCoder  |  0x3f题单
维度二(按算法):  DP · 搜索 · 图论 · 数据结构 · 字符串 · 前缀/双指针 · 数学/思维
```

- **LeetCode** 采用最细的「算法题型」分类(28 类,DP 再细分 10 个子类)。
- **Codeforces / AtCoder** 主要按「比赛」归档,辅以每日一题、构造专项、复习等场景化目录。
- **template/** 沉淀可复用算法模板,与刷题目录互补。
