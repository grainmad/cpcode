// 对拍暴力解骨架 —— 把题意直译成最笨的正确写法（O(n^2)/O(2^n) 都行，n 小）。
// 与 sol 读同样的输入格式、输出同样的答案；由 cmake/stress.cmake 驱动对比。
#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    // 读入（照抄 sol 的读法）
    int n;
    cin >> n;

    // 暴力求解：不做任何优化，直译题意
    // for (int i = 0; i < n; i++) ...

    // 输出（格式必须与 sol 完全一致）
    // cout << ans << "\n";
    return 0;
}
